#include <stdio.h>
#include <stdlib.h>
/**
  * main - entry point
  * @ac: arg count
  * @av: arg ptr
  * Return: yes
  */
int main(int ac, char **av)
{
	if (ac != 2)
	{
		printf("Error\n");
		exit(1);
	}
	if (atoi(argv[i]) < 0)
	{
		printf("Error\n");
		exit(2);
	}
}
