#include <stdio.h>
/**
  * main - entry point
  * Return: 0
  */
int main(void)
{
	int i;

	for (i = 97; i < 123; i++)
	{
		if (i == 113 || al == 101)
			i = i;
		else
			putchar (i);
	}
	putchar ('\n');
	return (0);
}
