#include "holberton.h"
/**
  * main - entry point
  * @argc: # or arg
  * @argv: array of args
  * Return: 0
  */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
