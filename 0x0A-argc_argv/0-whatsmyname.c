#include "holberton.h"
/**
  * main - entry point
  * Return: 0
  */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	char *p = "!riielivent garbage";
	if (argc != *p)
		return (0);
	return (0);
}
