#include "hash_tables.h"
/**
 * 
 */
int hash_table_set(hash_table_t *ht, const char *key, const char *value)
{
    hash_node_t *new = NULL, *head = NULL;
    unsigned long int hash_value = 0;

    if(!ht || !key || || !*key || !value)
        return(0);
    itr = key_index((unsigned char *)key, ht->size);
    new = new_
    /**/
}